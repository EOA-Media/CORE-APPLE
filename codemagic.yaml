workflows:
  ios-release:
    name: CORE iOS Release

    environment:
      node: 20

    scripts:
      - npm ci
      - npm run build
      - npx cap sync ios

    artifacts:
      - ios/**/*.ipa
      - ios/**/*.app

    publishing:
      email:
        recipients:
          - eoamediallc@gmail.com
